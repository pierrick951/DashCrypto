export type tabsNavBottype = {
  id: string;
  text: string;
  ico: JSX.Element;
}[];

export type tabsNavtype = {
  id: string;
  text: string;
  ico: JSX.Element;
  href: string;
  index: number;
}[];
