export type MainContentType = {
  comp: JSX.Element;
  text: string;
}[];

export type ArticleType = {
  id: string;
  title: string;
  text: string;
  img: string;
}[];

export type buttonType = {
  id: string;
  text: string;
  color: string;
}[];
